package ru.job4j.collections.generic.store;

public class RoleStore extends AbstractStore<Role> {

    /**
     * Стандартный конструктор.
     *
     * @param array - массив.
     * @param size  - размер массива.
     */
    public RoleStore(Role[] array, int size) {
        super(array, size);
    }
}
